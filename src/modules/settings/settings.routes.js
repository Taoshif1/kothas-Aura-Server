import { Router } from "express"; import verifyJWT from "../../middleware/verifyJWT.js"; import verifyAdmin from "../../middleware/verifyAdmin.js"; import asyncHandler from "../../utils/asyncHandler.js"; import { getSettings, updateSettings } from "./settings.controller.js"; const router=Router(); router.get("/",asyncHandler(getSettings)); router.patch("/",verifyJWT,verifyAdmin,asyncHandler(updateSettings)); export default router;
