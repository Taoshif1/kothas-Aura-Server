import{Router}from"express";import verifyJWT from"../../middleware/verifyJWT.js";import verifyAdmin from"../../middleware/verifyAdmin.js";import asyncHandler from"../../utils/asyncHandler.js";import{list,detail,status}from"./contact.controller.js";const r=Router();r.use(verifyJWT,verifyAdmin);r.get("/",asyncHandler(list));r.get("/:id",asyncHandler(detail));r.patch("/:id/status",asyncHandler(status));export default r;
