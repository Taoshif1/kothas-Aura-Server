import{Router}from"express";import verifyJWT from"../../middleware/verifyJWT.js";import verifyAdmin from"../../middleware/verifyAdmin.js";import asyncHandler from"../../utils/asyncHandler.js";import{list,detail,status,payment,cancel,stats}from"./adminOrder.controller.js";const r=Router();r.use(verifyJWT,verifyAdmin);r.get("/stats",asyncHandler(stats));r.get("/",asyncHandler(list));r.get("/:id",asyncHandler(detail));r.patch("/:id/status",asyncHandler(status));r.patch("/:id/payment",asyncHandler(payment));r.post("/:id/cancel",asyncHandler(cancel));export default r;
