import { ObjectId } from "mongodb";
import { getDatabase } from "../../config/mongodb.js";
import createSlug from "../../utils/createSlug.js";

const collection = () => getDatabase().collection("categories");
const httpError = (status, message) => Object.assign(new Error(message), { status });
const parseId = (id) => { if (!ObjectId.isValid(id)) throw httpError(400, "Invalid category id"); return new ObjectId(id); };
const sanitizeSubcategories = (items = []) => items.map((item) => ({ name: String(item?.name || "").trim(), slug: createSlug(item?.name) }));
const sanitize = (body = {}) => ({ ...(body.name !== undefined && { name: String(body.name).trim() }), ...(body.description !== undefined && { description: String(body.description).trim() }), ...(body.image !== undefined && { image: String(body.image).trim() }), ...(body.subcategories !== undefined && { subcategories: sanitizeSubcategories(body.subcategories) }), ...(body.active !== undefined && { active: body.active }) });
const validate = (category, partial = false) => { if (!partial && !category.name) throw httpError(400, "name is required"); if (category.name !== undefined && !category.name) throw httpError(400, "name must be a non-empty string"); if (category.active !== undefined && typeof category.active !== "boolean") throw httpError(400, "active must be a boolean"); if (category.subcategories !== undefined) { if (!Array.isArray(category.subcategories) || category.subcategories.some((item) => !item.name || !item.slug)) throw httpError(400, "Every subcategory requires a valid name"); const slugs = category.subcategories.map((item) => item.slug); if (new Set(slugs).size !== slugs.length) throw httpError(400, "Subcategory names must be unique within a category"); } };

export const getCategories = async (req, res) => { const categories = await collection().find({ active: { $ne: false } }).sort({ name: 1 }).toArray(); res.json({ categories, count: categories.length }); };
export const getAdminCategories = async (req, res) => { const categories = await collection().find({}).sort({ name: 1 }).toArray(); res.json({ categories, count: categories.length }); };
export const createCategory = async (req, res) => { const category = sanitize(req.body); validate(category); category.slug = createSlug(category.name); if (await collection().findOne({ slug: category.slug })) throw httpError(409, "Category slug already exists"); const now = new Date(); const document = { ...category, description: category.description || "", image: category.image || "", subcategories: category.subcategories || [], active: category.active ?? true, createdAt: now, updatedAt: now }; const result = await collection().insertOne(document); res.status(201).json({ category: { ...document, _id: result.insertedId } }); };
export const updateCategory = async (req, res) => { const changes = sanitize(req.body); if (!Object.keys(changes).length) throw httpError(400, "No valid category fields provided"); validate(changes, true); if (changes.name) changes.slug = createSlug(changes.name); const _id = parseId(req.params.id); if (changes.slug && await collection().findOne({ _id: { $ne: _id }, slug: changes.slug })) throw httpError(409, "Category slug already exists"); const result = await collection().findOneAndUpdate({ _id }, { $set: { ...changes, updatedAt: new Date() } }, { returnDocument: "after" }); if (!result) throw httpError(404, "Category not found"); res.json({ category: result }); };
export const deleteCategory = async (req, res) => { const result = await collection().findOneAndUpdate({ _id: parseId(req.params.id) }, { $set: { active: false, updatedAt: new Date() } }, { returnDocument: "after" }); if (!result) throw httpError(404, "Category not found"); res.json({ category: result }); };
